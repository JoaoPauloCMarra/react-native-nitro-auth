///
/// HybridNativeAuthAdapterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `AuthNonce` to properly resolve imports.
namespace margelo::nitro::NitroAuth { struct AuthNonce; }
// Forward declaration of `ProviderUserResult` to properly resolve imports.
namespace margelo::nitro::NitroAuth { struct ProviderUserResult; }
// Forward declaration of `AuthProvider` to properly resolve imports.
namespace margelo::nitro::NitroAuth { enum class AuthProvider; }
// Forward declaration of `LoginOptions` to properly resolve imports.
namespace margelo::nitro::NitroAuth { struct LoginOptions; }
// Forward declaration of `ProviderTokenResult` to properly resolve imports.
namespace margelo::nitro::NitroAuth { struct ProviderTokenResult; }
// Forward declaration of `ProviderVoidResult` to properly resolve imports.
namespace margelo::nitro::NitroAuth { struct ProviderVoidResult; }

#include "AuthNonce.hpp"
#include "ProviderUserResult.hpp"
#include <NitroModules/Promise.hpp>
#include "AuthProvider.hpp"
#include "LoginOptions.hpp"
#include <optional>
#include <string>
#include <vector>
#include "ProviderTokenResult.hpp"
#include "ProviderVoidResult.hpp"

namespace margelo::nitro::NitroAuth {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NativeAuthAdapter`
   * Inherit this class to create instances of `HybridNativeAuthAdapterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNativeAuthAdapter: public HybridNativeAuthAdapterSpec {
   * public:
   *   HybridNativeAuthAdapter(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNativeAuthAdapterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNativeAuthAdapterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNativeAuthAdapterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual AuthNonce createNonce() = 0;
      virtual std::shared_ptr<Promise<ProviderUserResult>> login(AuthProvider provider, const std::optional<LoginOptions>& options) = 0;
      virtual std::shared_ptr<Promise<ProviderUserResult>> requestScopes(const std::vector<std::string>& scopes) = 0;
      virtual std::shared_ptr<Promise<ProviderTokenResult>> refreshToken() = 0;
      virtual std::shared_ptr<Promise<ProviderUserResult>> silentRestore() = 0;
      virtual std::shared_ptr<Promise<ProviderVoidResult>> revokeAccess(AuthProvider provider) = 0;
      virtual bool hasPlayServices() = 0;
      virtual void invalidatePendingOperations() = 0;
      virtual void cancel() = 0;
      virtual void logout() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NativeAuthAdapter";
  };

} // namespace margelo::nitro::NitroAuth
